多线程访问同一个共享变量时特别容易出现并发问题，
特别是多个线程需要对一个共享变量进行写入时。
为了保证线程安全，一般使用者在访问变量时需要进行适当的同步。

ThreadLocal
当创建一个变量后，每个线程对其进行访问的时候，访问的是自己线程的变量。
它提供了线程本地变量，如果创建了一个ThreadLocal变量，
那么访问这个变量的每个线程都会有这个变量的一个本地副本。
当多线程操作这个变量时，实际操作的是自己本地内里面的变量，从而避免了线程安全问题。
